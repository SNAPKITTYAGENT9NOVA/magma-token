{
  "name": "magma-token",
  "version": "0.1.0",
  "description": "MAGMA (MGM) — Native token of the SnapKitty SACM mesh. Emission tied to WORM-sealed productive work.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network base-sepolia",
    "deploy:base": "hardhat run scripts/deploy.js --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "hardhat": "^2.22.0",
    "dotenv": "^16.0.0"
  },
  "license": "MIT"
}
